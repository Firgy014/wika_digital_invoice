<odoo>
    <data>
        <record id="view_amount_scf_price_cut_form" model="ir.ui.view">
            <field name="name">amount.scf.price.cut.form</field>
            <field name="model">wika.amount.scf.wizard</field>
            <field name="arch" type="xml">
                <form string="Masukan Nilai Potongan SCF">
                    <group>
                        <field name="invoice_id" readonly="1"/>
                        <field name="amount"/>
                    </group>
                    <footer>
                        <button string="Submit Nilai Potongan" type="object" class="btn-primary" name="action_save"/>
                        <button string="Cancel" class="btn-secondary" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>

        <record id="action_amount_scf_price_cut" model="ir.actions.act_window">
            <field name="name">Amount SCF Price Cut</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">wika.amount.scf.wizard</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
        </record>
    </data>
</odoo>