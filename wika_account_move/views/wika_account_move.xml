<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <data>

        <!-- form -->
        <record id="view_wika_account_move_form_inherit" model="ir.ui.view">
            <field name="name">wika.account.move.inherit.view.form</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_move_form"/>
            <field name="arch" type="xml">
                <!-- state -->
                <xpath expr="//field[@name='state']" position="attributes">
                    <attribute name="statusbar_visible">draft,upload,request,posted</attribute>
                </xpath>

                <!-- button -->
                <xpath expr="/form/header/button[@name='action_post']" position="after">
                    <button name="action_submit" string="SUBMIT" type="object" class="oe_highlight" 
                    attrs="{'invisible': ['|', '|', ('state', '=', 'upload'), ('state', '=', 'request'), 
                        ('state', '=', 'posted')]}"/>
                </xpath>
                <xpath expr="/form/header/button[@name='action_submit']" position="after">
                    <button name="action_request" string="REQUEST" type="object" class="oe_highlight" 
                    attrs="{'invisible': ['|', '|', ('state', '=', 'draft'), ('state', '=', 'request'), 
                        ('state', '=', 'posted')]}"/>
                </xpath>
                <xpath expr="/form/header/button[@name='action_submit']" position="before">
                    <button name="action_post" string="CONFIRM" type="object" class="oe_highlight" 
                    attrs="{'invisible': ['|', '|', ('state', '=', 'draft'), ('state', '=', 'upload'), 
                        ('state', '=', 'posted') ]}"/>
                </xpath>
                <xpath expr="/form/header/button[@name='action_submit']" position="after">
                    <button name="action_reject" string="REJECT" type="object" class="oe_highlight" 
                    attrs="{'invisible': ['|', '|', ('state', '=', 'draft'), ('state', '=', 'upload'), 
                        ('state', '=', 'posted') ]}"/>
                </xpath>
                <!-- field -->
                <xpath expr="//field[@name='partner_id']" position="after">
                    <field name="bap_id"/>
                    <field name="pr_id"/>
                </xpath>
                <xpath expr="//field[@name='invoice_date']" position="after">
                    <field name="branch_id"/>
                    <field name="department_id"/>
                    <field name="project_id"/>
                    <field name="step_approve"/>
                </xpath>
                <xpath expr="//notebook/page[@name='other_info']" position="after">
                    <page string="List Document">
                        <field name="document_ids">
                            <tree>
                                <field name="document_id"/>
                                <field name="document"/>
                                <field name="filename" invisible="1"/>
                                <field name="state"/>
                            </tree>
                        </field>
                    </page>
                    <page string="List Approval">
                        <field name="history_approval_ids">
                            <tree string="List Approval">
                                <field name="user_id"/>
                                <field name="groups_id"/>
                                <field name="date"/>
                                <field name="note"/>
                            </tree>
                        </field>
                    </page>
                </xpath>
            </field>
        </record>
    </data>
    
</odoo>
